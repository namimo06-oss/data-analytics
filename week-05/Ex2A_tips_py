# LAB 1

# Define known values
food_cost = 79.25
tax = 6.54
tip = 12.00
# Calculate the unknown
total_due = food_cost + tax + tip
# Display the results
# print("The total due is " + str(total_due))

#1. The str() funtions is used to convert a value into a string (text). It is being used to convert the total value into test,
# so when running the code there is not error when adding a text and a value.


print("Food cost is " + str(food_cost) + " and tax is " + str(tax)) # make sure there is space when need it between the quotes 
print("Tip is " + format(tip, ".2f"))
print("Total due is " + str(total_due))


