
# enumerate

grocery_list = ["peach","apple","chocolate chip cookies","rotisserie chicken", "pita"]

for number, thing in enumerate(grocery_list,start =1):
    #print(i,thing)
    if number > 3: #condition should be first because python reads from top to bottom
        break
    print(f'Item number {number} in the list is {thing}')


